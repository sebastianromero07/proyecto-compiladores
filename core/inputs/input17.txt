#include <stdio.h>

int main() {
    int r;

    r = 0;

    if (3 * 4 < 2 + 5) {
        r = 1;
        printf("Valor de r = %d\n", r);
    } else {
        r = 2;
        printf("Valor de r = %d\n", r);
    }

    printf("Valor de r = %d\n", r);

    return 0;
}
