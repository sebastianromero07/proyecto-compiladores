#include <stdio.h>

float promedio(int a, float b) {
    float r;
    r = (a + b) / 2.0;
    return r;
}

int main() {
    int x;
    float y;
    float p;

    x = 4;
    y = 6.0;
    p = promedio(x, y);

    printf("promedio = %f\n", p);
    return 0;
}
