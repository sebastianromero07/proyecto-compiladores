#include <stdio.h>

int cuadrado(int x) {
    int r;
    r = x * x;
    return r;
}

int main() {
    int a;
    int q;

    a = 3;
    q = cuadrado(a);

    printf("cuadrado = %d\n", q);  
    return 0;
}
