#include <stdio.h>

int main() {
    int x;
    int y;
    int max;

    x = 7;
    y = 4;

    if (x > y) {
        max = x;
    } else {
        max = y;
    }

    printf("max = %d\n", max);   
    return 0;
}
